# npm version bumps for twikoo-vercel are handled by auto-update.yml.
# CVE notifications come from Dependabot alerts (repo setting), not this file.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
